#include "symbol-table.h"
#include <stdlib.h>

symbol_table *add_symbol(symbol_table *table, char *name, uint64_t value) {
  return NULL;
}

symbol find_symbol_by_name(symbol_table *table, char *name) {
  symbol result = {NULL, 0};
  return result;
}

symbol find_symbol_by_value(symbol_table *table, uint64_t value) {
  symbol result = {NULL, 0};
  return result;
}
